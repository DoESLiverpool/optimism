const ModelIemsBase = require('./modelItemsBase');

class CalendarItems extends ModelIemsBase {
    constructor(model) {
        super(model);
    }

    get(startDate, endDate, resourceId) {

    }
}

module.exports = CalendarItems;
